lambda基本格式
1. 不需要参数
() -> 5             返回值为 5

2. 接收一个参数
x -> 2 * x          返回其2倍的值

3. 接受2个参数
(x, y) -> x – y             返回运算结果

4. 接收2个有类型定义参数
(int x, int y) -> x + y      返回运算结果

5. 接受一个有类型定义参数
(String s) -> System.out.print(s)   不返回任何结果