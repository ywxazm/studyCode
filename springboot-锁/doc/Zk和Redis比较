一.  ZK的问题
    1.GC停顿, 使唤zk服务器检测不到客户端心跳, 误删除临时节点, 导致其它线程获取到锁
        JVM优化, 但不能够根本解决问题

    2.无数据无法同步的问题, 2段提交保证

二.  Redis的问题
    1.原生Redis锁的过期时间(默认30s) < 业务时间时, 会导致锁失效
        使用Redisson框架使用Watch Dog, 引入自动延时机制, 使锁不过期

    2.客户端向master获取锁后, master未与slave同步就宕机, 导致数据不同步, 其它线程亦可获取此锁